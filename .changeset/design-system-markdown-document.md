---
"fabric-app": patch
---

Add Design System (`design.md`) as a generated project document.

The new document type is available from project onboarding and the Documents tab, uses an evidence-grounded SYSTEM prompt with explicit `TBD` handling, participates in RAG retrieval and embedding, and is exposed through the application API.
