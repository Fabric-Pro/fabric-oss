---
"fabric-app": patch
---

The Context tab's "Remove duplicates" flow now lists which items are duplicates and which item each one is a copy of, both in the banner and in the confirmation dialog, before anything is deleted.

Fizzy #2619 follow-up: the banner and confirm dialog previously showed only a count.
