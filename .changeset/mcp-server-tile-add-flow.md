---
"fabric-app": patch
---

Clicking the add action on an MCP server tile on the Connections page now opens the configuration dialog directly for that server with parameters pre-filled, or opens the registry search dialog when the server is not an exact match.
