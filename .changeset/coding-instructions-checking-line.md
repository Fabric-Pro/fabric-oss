---
"fabric-app": patch
---

The Coding Instructions tab shows "Checking your upload" as a highlighted status with a spinner instead of a plain sentence under the summary, and no longer keeps a deleted file selected after the version that removed it is published.
