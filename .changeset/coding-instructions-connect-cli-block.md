---
"fabric-app": patch
---

The Coding Instructions "Connect your agent" dialog now leads with the CLI route for Claude Code (sign in, then `fabric instructions init`) as its own copyable step, presents the MCP configuration as the alternative, and offers the command before the first version is published as well.
