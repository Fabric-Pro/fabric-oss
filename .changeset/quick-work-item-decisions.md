---
"fabric-app": patch
---

Use the configured decision model to classify work items as bugs or features, with regular AI classification as fallback when Vercel AI Gateway is unavailable or the decision is uncertain.
