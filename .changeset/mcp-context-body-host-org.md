---
"fabric-app": patch
---

The `fabric_get_project_context` and `fabric_get_feature_decisions` MCP tools now return crawled URL text, captured channel conversations, and decision log entries to invited guests from another organization, instead of an empty result.
