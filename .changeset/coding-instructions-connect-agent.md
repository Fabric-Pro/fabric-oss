---
"fabric-app": patch
---

The Coding Instructions tab now has a "Connect your agent" button that mints a read-only API key and hands you a ready-to-paste MCP configuration for your coding tool, so developers can start pointing coding agents at a project's published instructions without leaving the tab.

The button opens the same dialog already used to connect a coding tool to a project's general context, with a starter sentence scoped to the published coding instructions instead. The project's MCP gateway docs now list the three instruction tools and show how the button fits into setup.
