---
"fabric-app": patch
---

A coding agent connected to the Fabric MCP gateway is now told how to bootstrap a project's context — setting its description and pushing the README and key docs from the working tree — and the MCP gateway documentation describes the same flow.
