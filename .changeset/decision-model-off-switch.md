---
"fabric-app": patch
---

Organization AI settings can now switch the decision model off, so work-item classification and action-item routing use the organization's regular AI model instead of the typed decision model.

Switching off is a distinct choice from clearing the preference: the Decisions row keeps a Reset action, and using it returns the organization to the default decision model rather than leaving decisions disabled.
