import { GitLabBrandIcon } from "@saas/data-connections/components/ProviderIcon";

export function GitLabIcon({ className }: { className?: string }) {
	return <GitLabBrandIcon className={className} />;
}
